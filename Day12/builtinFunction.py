# built-in-function

# int(), float(), str(), list(), set(), dict() - ~~화 하기
# type() - 타입 알려주기
# range() - m ~ n - 1 숫자 순회
# enumerate() - 리스트를 넣으면 순서와 요소를 돌려줌
# zip() - 두 리스트를 짝을 지어줌


# operator(연산자)

# 논리 연산자
# and, or, not

# 멤버쉽(in) 연산자
# ? in 리스트, 문자열 있니?


# dataType

# int, float, bool

# str[문자열]
# +: 문자 연결, *: 문자 n배 [문자열 연산자]
# is시리즈, split(str->list), count, replace(old, new), join()

# list[리스트]
# 요소 중복 가능, any 타입 가능
# append, sort, reverse, count, insert, remove

# dict[딕셔너리]
# key(고유) - value
# {}

# set[셋]
# 중복 안 됨, {}
# 리스트 -> 세트 -> 리스트 (중복 제거)
